# Standalone test (no Catch2 needed — has its own main())
add_executable(test_calculators test_calculators.cpp)
target_compile_features(test_calculators PRIVATE cxx_std_20)
target_include_directories(test_calculators PRIVATE ${CMAKE_SOURCE_DIR}/include)
if(CMAKE_CXX_COMPILER_ID MATCHES "MSVC")
    target_compile_options(test_calculators PRIVATE /W4)
elseif(CMAKE_CXX_COMPILER_ID MATCHES "GNU|Clang")
    target_compile_options(test_calculators PRIVATE -Wall -Wextra)
endif()
add_test(NAME test_calculators COMMAND test_calculators)

# Standalone core tests (fields, nuclear, operators, solver — no Catch2)
add_executable(test_navier_core test_navier_core.cpp)
target_link_libraries(test_navier_core PRIVATE sdt_navier)
target_compile_features(test_navier_core PRIVATE cxx_std_20)
target_include_directories(test_navier_core PRIVATE ${CMAKE_SOURCE_DIR}/include)
if(CMAKE_CXX_COMPILER_ID MATCHES "MSVC")
    target_compile_options(test_navier_core PRIVATE /W4)
elseif(CMAKE_CXX_COMPILER_ID MATCHES "GNU|Clang")
    target_compile_options(test_navier_core PRIVATE -Wall -Wextra)
endif()
add_test(NAME test_navier_core COMMAND test_navier_core)

find_package(Catch2 QUIET)

if(Catch2_FOUND)
    add_executable(test_fields test_fields.cpp)
    target_link_libraries(test_fields PRIVATE sdt_navier Catch2::Catch2)
    add_test(NAME test_fields COMMAND test_fields)

    add_executable(test_operators test_operators.cpp)
    target_link_libraries(test_operators PRIVATE sdt_navier Catch2::Catch2)
    add_test(NAME test_operators COMMAND test_operators)

    add_executable(test_solver test_solver.cpp)
    target_link_libraries(test_solver PRIVATE sdt_navier Catch2::Catch2)
    add_test(NAME test_solver COMMAND test_solver)

    add_executable(test_nuclear test_nuclear.cpp)
    target_link_libraries(test_nuclear PRIVATE sdt_navier Catch2::Catch2)
    add_test(NAME test_nuclear COMMAND test_nuclear)
else()
    message(WARNING "Catch2 not found, tests will not be built")
endif()

